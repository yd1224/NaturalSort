using NaturalSort.Helpers;
using NaturalSort.SortingMethods;
using NaturalSort.Tests;
using System.Text;

class Program
{
    const long _2GB = 2L * 1024 * 1024 * 1024; //size of total integers in bytes (not the file size)
    const long _3GB = 3L * 1024 * 1024 * 1024; //size of total integers in bytes (not the file size)
    static async Task Main()
    {
        //await TestModifiedSort.TestSort(_2GB);
        //await TestUnmodifiedSort.TestSort(100);
    }
}
